apiVersion: v1
kind: Service
metadata:
  name: {{ include "sboot.fullname" . }}
  labels:
    {{- include "sboot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sboot.selectorLabels" . | nindent 4 }}
