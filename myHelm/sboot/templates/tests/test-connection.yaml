apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sboot.fullname" . }}-test-connection"
  labels:
    {{- include "sboot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sboot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
